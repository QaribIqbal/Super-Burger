/**
 * Hero — legacy component, superseded by ScrollySection.
 * Kept as a stub so any external references don't break the build.
 * See components/ScrollySection.tsx for the active scrollytelling hero.
 */
export default function Hero() {
  return null;
}